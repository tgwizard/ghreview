name: Publish to npm

# Publishes with `--provenance` so npm records a signed attestation linking
# this version to this GitHub workflow + commit. Users can verify with
# `npm audit signatures` after install. Requires that the npm package be
# configured with a trusted publisher pointing at this repo/workflow (set
# at npmjs.com → Package settings → Publishing access → Trusted publishers).
#
# Third-party actions are pinned to commit SHAs with the tag as a comment.
# Refresh with `git ls-remote --tags https://github.com/actions/<name>`.

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write # Required for npm provenance via GitHub OIDC.

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Setup Node
        # Node 24 ships with npm 11+; OIDC-based registry auth for
        # Trusted Publishers needs ≥ 11.5.1. Node 22 (npm 10.x) would
        # sign provenance correctly but 404 on the PUT because it
        # falls back to an empty NODE_AUTH_TOKEN.
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Install (strict lockfile)
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

      - name: Publish
        run: npm publish --provenance --access public
